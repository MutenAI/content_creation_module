import os, requests; from dotenv import load_dotenv; load_dotenv(); key = os.getenv('TAVILY_API_KEY'); print(f'Test Tavily Search API: ', end=''); response = requests.post('https://api.tavily.com/search', headers={'Authorization': f'Bearer {key}'}, json={'query': 'test', 'search_depth': 'basic'}); print(f'[{response.status_code}] ' + ('[OK]' if response.status_code == 200 else '[ERRORE]')); print(f'Response: {response.text[:200]}...' if response.status_code != 200 else '')
